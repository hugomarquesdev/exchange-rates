###### This project is a frontend challenge from Uphold.

# Exchange Rates

Exchange rates for the different currencies supported by Uphold.

## Installation

```bash
1. Install dependencies (`yarn install `)
2. `yarn start`

To build use `yarn build`.
